<UserControl x:Class="TryMVVM_SchoolManagement.Views.ClassesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TryMVVM_SchoolManagement.Views"
             xmlns:models="clr-namespace:TryMVVM_SchoolManagement.Models"
             xmlns:services="clr-namespace:TryMVVM_SchoolManagement.Services"
             services:ViewModelLocator.AutoHookedUpViewModel="True"
             
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <DataTemplate DataType="{x:Type models:Class}">

            <StackPanel Orientation = "Horizontal">
                <TextBlock Text = "{Binding Path = Id, Mode = OneWay}" 
                    Width = "10" Margin = "3 5 3 5"/>

                <TextBlock Text = "{Binding Path = Name, Mode = OneWay}" 
                    Width = "100" Margin = "3 5 3 5"/>
            </StackPanel>

        </DataTemplate>
    </UserControl.Resources>
    
    <Grid>
        <ListBox ItemsSource="{Binding ClassList}"
                 Width="200"
                 Background="AliceBlue"
                 SelectedItem="{Binding SelectedClass}"/>
    </Grid>
    
</UserControl>
